import { BrowserRouter, Routes, Route } from 'react-router-dom'
import  from '..//'
import  from '..//'


const Router = () => {
    return (
        <>
            <BrowserRouter>
                <Routes>
                    <Route element={< />} path='' />
                    <Route element={< />} path='' />
                </Routes>
            </BrowserRouter>
        </>

    )
}

export default Router